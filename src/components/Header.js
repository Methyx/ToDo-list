import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const Header = () => {
  return (
    <header>
      <FontAwesomeIcon className="icon" icon="rectangle-list" />
      <h1>ToDo List</h1>
    </header>
  );
};

export default Header;
